#include "main.h"

/**
 * _islower - the check for lowercase character
 * @b: the character to be checked
 * Return: 1 if b is lowercase, 0 otherwise
 */
int _islower(int b)
{
        return (b >= 'a' && b <= 'z');

}

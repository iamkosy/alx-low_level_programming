#include "main.h"

/**
 * _isdigit - will check for digits from 0 through 9
 * @a: check int
 * Return: 1 if a is a digit, 0 otherwise
 */
int _isdigit(int a)
{
        return (a >= '0' && a <= '9');
}
